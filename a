ame:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
    Name = "감옥 펌프 오토팜 (Prison Farm)",
    LoadingTitle = "Alpaca's Script Interface",
    LoadingSubtitle = "by Alpaca", -- 제작자 이름 변경
    ConfigurationSaving = {
        Enabled = true,
        FolderName = nil, 
        FileName = "PrisonFarmConfig"
    },
    Discord = {
        Enabled = false,
        Invite = "noinvitelink",
        RememberJoins = true
    },
    KeySystem = true,
    KeySettings = {
        Title = "알파 허브 입니다",
        Subtitle = "Key System",
        Note = "No method of obtaining the key is provided",
        FileName = "키",
        SaveKey = true,
        GrabKeyFromSite = false,
        Key = {"알파카"}
    }
})

table.insert(getgenv().RayfieldWindows, Window)

-- 메인 탭 생성
local FarmTab = Window:CreateTab("오토팜", 4483362458)
local FarmSection = FarmTab:CreateSection("농장 기능")

-- 변수 정의
local AutoFarmEnabled = false
local FarmDelay = 1 -- 기본 지연 시간


-- 오토팜 토글
local AutoFarmToggle = FarmTab:CreateToggle({
    Name = "자동 고기 파밍 켜기",
    CurrentValue = false,
    Flag = "AutoFarmToggle",
    Callback = function(Value)
        AutoFarmEnabled = Value
        
        if AutoFarmEnabled then
            Rayfield:Notify({
                Title = "활성화",
                Content = "오토팜이 시작되었습니다.",
                Duration = 3,
                Image = 4483362458
            })
            
            task.spawn(function()
                local lp = game.Players.LocalPlayer
                
                while AutoFarmEnabled do
                    -- [1단계] 고기 획득 위치로 이동 및 광클
                    if lp and lp.Character and lp.Character:FindFirstChild("HumanoidRootPart") then
                        lp.Character.HumanoidRootPart.CFrame = CFrame.new(Vector3.new(-60.26, 4.70, -61.24))
                        task.wait(0.2) -- 텔레포트 지연
                    end

                    -- 고기 획득을 위해 0.1초 간격으로 20번 광클 시도
                    for i = 1, 20 do
                        if not AutoFarmEnabled then break end
                        -- 원격 이벤트(RemoteEvent) 또는 클릭을 통한 획득 시도
                        -- (게임 환경에 따라 fireclickdetector나 RemoteEvent를 사용하세요)
                        task.wait(0.1)
                    end
                    
                    task.wait(0.5) -- 다음 단계로 넘어가기 전 짧은 대기

                    -- [2단계] 먹기 위치로 이동
                    if AutoFarmEnabled and lp and lp.Character and lp.Character:FindFirstChild("HumanoidRootPart") then
                        lp.Character.HumanoidRootPart.CFrame = CFrame.new(Vector3.new(-12.36, 4.68, -64.56))
                        task.wait(0.2)
                    end

                    -- [3단계] 먹는 동작 수행 및 제자리 고정 (TP 버그 방지)
                    -- 먹기 동작이 끝날 때까지 7초 동안 다른 동작을 멈춤
                    local eatDuration = 7
                    local startTime = tick()
                    while AutoFarmEnabled and (tick() - startTime) < eatDuration do
                        task.wait(1)
                    end

                    -- 슬라이더에 설정된 지연 시간 적용
                    if AutoFarmEnabled then
                        task.wait(FarmDelay)
                    end
                end
            end)
        else
            Rayfield:Notify({
                Title = "비활성화",
                Content = "오토팜이 중지되었습니다.",
                Duration = 3,
                Image = 4483362458
            })
        end
    end
})



Rayfield:Notify({
    Title = "성공",
    Content = "Alpaca의 오토팜 UI가 성공적으로 로드되었습니다!",
    Duration = 5,
    Image = 4483362458
})
