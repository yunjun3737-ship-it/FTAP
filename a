local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
    Name = "감옥 펌프 오토팜 (Prison Farm)",
    LoadingTitle = "스크립트 인터페이스",
    LoadingSubtitle = "by Gemini",
    ConfigurationSaving = {
        Enabled = true,
        FolderName = nil, 
        FileName = "PrisonFarmConfig"
    },
    Discord = {
        Enabled = false,
        Invite = "noinvitelink",
        RememberJoins = true
    },
    KeySystem = false, -- 키 시스템 사용 안 함
    KeySettings = {
        Title = "Untitled",
        Subtitle = "Key System",
        Note = "No method of obtaining the key is provided",
        FileName = "Key",
        SaveKey = true,
        GrabKeyFromSite = false,
        Key = {"Hello"}
    }
})

-- 메인 탭 생성
local FarmTab = Window:CreateTab("오토팜 (Auto Farm)", 4483362458)

-- 섹션 추가
local FarmSection = FarmTab:CreateSection("농장 기능")

-- 오토팜 토글 (기능 제외)
local AutoFarmToggle = FarmTab:CreateToggle({
    Name = "자동 고기 파밍 켜기",
    CurrentValue = false,
    Flag = "AutoFarmToggle",
    Callback = function(Value)
        if Value then
            -- 기능이 켜졌을 때 실행될 코드 자리
            print("오토팜이 활성화되었습니다.")
        else
            -- 기능이 꺼졌을 때 실행될 코드 자리
            print("오토팜이 비활성화되었습니다.")
        end
    end
})

-- 슬라이더 추가 (작업 속도/지연 시간 조절용)
local FarmSpeedSlider = FarmTab:CreateSlider({
    Name = "파밍 지연 시간 (초)",
    Range = {0.1, 5},
    Increment = 0.1,
    Suffix = "s",
    CurrentValue = 1,
    Flag = "FarmSlider",
    Callback = function(Value)
        print("지연 시간이 " .. Value .. "초로 변경되었습니다.")
    end
})

-- 경고 알림 표시
Rayfield:Notify({
    Title = "성공",
    Content = "오토팜 UI가 성공적으로 로드되었습니다!",
    Duration = 5,
    Image = 4483362458
})
